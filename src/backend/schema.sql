-- Render-compatible placeholder.
-- The real schema is at `infinite/backend/schema.sql`.
-- Your backend code already attempts to load `schema.sql` from its own directory.
-- Since Render runs from `src/backend`, we include a copy by referencing the real schema is not possible here.
-- Replace this file with the contents of `infinite/backend/schema.sql` if you want Render to auto-init DB.

-- NOTE: This placeholder prevents missing-file crashes; it does not create tables.

