# TODO - 2016 simple UI refactor

- [x] Step 1: Refactor `frontend/style.css` to a simpler 2016-inspired palette and remove glass/glow effects.

- [x] Step 2: Fix CSS variable duplication (e.g., duplicate `--primary`) and normalize font-weight usage.

- [x] Step 3: Move key inline styles from `frontend/index.html` into new/updated CSS classes in `frontend/style.css` (urgency banner, trust sections, alerts, and button overrides used inline).

- [x] Step 4: Update `frontend/index.html` to use the new classes instead of inline `style="..."`.

- [x] Step 5: Do a quick local sanity check by opening `frontend/index.html` and verifying layout responsiveness.


