# TODO
- [x] Edit `frontend/admin.css` to apply Windows 95/NT corporate intranet theme (remove glassmorphism, blur, transparency, gradients, rounded corners, shadows; apply required colors, borders, classic tables, beveled buttons, square inputs).
- [x] Edit `frontend/admin.html` to add top header date/time element + top logout link, and adjust sidebar item label.
- [x] Add minimal date/time population logic in `frontend/admin.js` (populate `#topbarDateTime` every second; do not change existing event listeners/IDs).
- [x] Style `#topbarLogoutBtn` and `#topbarDateTime` in `frontend/admin.css` to match 1998–2002 look.
- [ ] Sanity-check: login, sidebar navigation, table rendering + action buttons, modal update status, settings save.

